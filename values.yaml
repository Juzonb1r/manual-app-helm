replicaCount: 1

image:
  repository: juzonb1r/my-website
  tag "latest"
  pullPolicy: Always

service:
  type: NodePort
  port: 3000

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3

